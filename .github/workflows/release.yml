# DyberVPN Release Pipeline
# Copyright 2026 Dyber, Inc.

name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write
  packages: write

jobs:
  # Build binaries for each platform
  build-binaries:
    name: Build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact: dybervpn
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            artifact: dybervpn
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }} --bin dybervpn
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Package
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          mkdir -p dist
          cp target/${{ matrix.target }}/release/${{ matrix.artifact }} dist/
          cp README.md dist/
          [ -f LICENSE ] && cp LICENSE dist/ || true
          [ -f LICENSE.md ] && cp LICENSE.md dist/ || true
          [ -f CHANGELOG.md ] && cp CHANGELOG.md dist/ || true
          cd dist
          tar -czf ../dybervpn-${VERSION}-${{ matrix.target }}.tar.gz *

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dybervpn-${{ matrix.target }}
          path: dybervpn-*.tar.gz

  # musl static build (best-effort — does not block release)
  build-musl:
    name: Build (x86_64-unknown-linux-musl)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Install musl tools
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools musl-dev pkg-config

      - uses: Swatinem/rust-cache@v2
        with:
          key: x86_64-unknown-linux-musl

      - name: Build
        run: cargo build --release --target x86_64-unknown-linux-musl --bin dybervpn
        env:
          CC_x86_64_unknown_linux_musl: musl-gcc
          AR_x86_64_unknown_linux_musl: ar

      - name: Package
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          mkdir -p dist
          cp target/x86_64-unknown-linux-musl/release/dybervpn dist/
          cp README.md dist/
          [ -f LICENSE ] && cp LICENSE dist/ || true
          [ -f LICENSE.md ] && cp LICENSE.md dist/ || true
          [ -f CHANGELOG.md ] && cp CHANGELOG.md dist/ || true
          cd dist
          tar -czf ../dybervpn-${VERSION}-x86_64-unknown-linux-musl.tar.gz *

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dybervpn-x86_64-unknown-linux-musl
          path: dybervpn-*.tar.gz

  # Build and push Docker image (non-blocking — GHCR may not be configured)
  docker:
    name: Docker
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./deploy/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/dybervpn:${{ steps.version.outputs.version }}
            ghcr.io/${{ github.repository_owner }}/dybervpn:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Create GitHub Release with all artifacts
  release:
    name: Create Release
    needs: [build-binaries]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd artifacts
          sha256sum *.tar.gz > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - name: Get version
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: DyberVPN v${{ steps.version.outputs.version }}
          body: |
            ## DyberVPN v${{ steps.version.outputs.version }}

            ### Post-Quantum VPN & Zero Trust Network Access

            ### Cryptographic Algorithms
            - **Key Exchange**: ML-KEM-768 + X25519 (FIPS 203)
            - **Authentication**: ML-DSA-65 (FIPS 204) / Ed25519
            - **Encryption**: ChaCha20-Poly1305

            ### Installation

            ```bash
            # Download and extract
            tar -xzf dybervpn-${{ steps.version.outputs.version }}-x86_64-unknown-linux-gnu.tar.gz
            sudo mv dybervpn /usr/local/bin/

            # Or use Docker
            docker pull ghcr.io/${{ github.repository_owner }}/dybervpn:${{ steps.version.outputs.version }}
            ```
          files: |
            artifacts/*.tar.gz
            artifacts/SHA256SUMS.txt
          draft: false
          prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') || contains(github.ref, 'rc') }}

  # PQ-sign release artifacts (optional — requires MLDSA_SIGNING_KEY secret)
  sign:
    name: PQ Sign Artifacts
    needs: [build-binaries, release]
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Check signing key
        id: check_key
        env:
          SIGNING_KEY: ${{ secrets.MLDSA_SIGNING_KEY }}
        run: |
          if [ -z "$SIGNING_KEY" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "No MLDSA_SIGNING_KEY configured, skipping PQ signing"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - uses: dtolnay/rust-toolchain@stable
        if: steps.check_key.outputs.skip != 'true'

      - uses: Swatinem/rust-cache@v2
        if: steps.check_key.outputs.skip != 'true'

      - name: Build dybervpn CLI
        if: steps.check_key.outputs.skip != 'true'
        run: cargo build --release --bin dybervpn

      - name: Download all artifacts
        if: steps.check_key.outputs.skip != 'true'
        uses: actions/download-artifact@v4
        with:
          path: artifacts/
          merge-multiple: true

      - name: PQ-sign artifacts with ML-DSA-65
        if: steps.check_key.outputs.skip != 'true'
        env:
          MLDSA_SIGNING_KEY_B64: ${{ secrets.MLDSA_SIGNING_KEY }}
        run: |
          echo "$MLDSA_SIGNING_KEY_B64" | base64 -d > signing.mldsa.key

          for f in artifacts/*.tar.gz; do
            echo "Signing: $f"
            ./target/release/dybervpn sign "$f" --key signing.mldsa.key
          done

          shred -u signing.mldsa.key 2>/dev/null || rm -f signing.mldsa.key

      - name: Upload signatures to release
        if: steps.check_key.outputs.skip != 'true'
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.sig.mldsa
